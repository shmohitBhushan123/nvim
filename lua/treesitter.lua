require'nvim-treesitter.configs'.setup {
    ensure_installed = { "go", "groovy", "bash", "lua", "python", "javascript", "yaml" },
    highlight = {
        enable = true,  -- Enable syntax highlighting
    },
}

